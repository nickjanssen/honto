
import { Component } from 'react';

export default class ArticleLink extends Component {
    render() {
        return (
            <div>
                {this.props.title}
            </div>
        );
    }
}